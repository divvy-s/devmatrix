# Devmatrix

This project contains separate directories for the backend and frontend.

## Structure

- `backend/`: Fastify API, BullMQ workers, Drizzle ORM, and shared packages.
- `frontend/`: (To be implemented)

## Backend

To get started with the backend:

```bash
cd backend
pnpm install
pnpm dev
```
